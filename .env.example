# Vortex Web → Vortex Core

# Local / Vite proxy defaults
VITE_API_URL=/api/v1
VITE_WS_URL=
VITE_USE_MSW=false

# Agent install script (Hosts → Install agent)
VITE_AGENT_CORE_URL=ws://127.0.0.1:8000
# Binary URL is auto: <current-web-origin>/agent
# Override only for a CDN: VITE_AGENT_BINARY_BASE_URL=https://cdn.example/agent
# Local/dev: cd ../VortexAgent && make publish-web
# Production Docker build compiles agents into the image automatically.

# Production (see .env.production.example / Docker build args):
# VITE_API_URL=https://api.vortex.timant32.ru/api/v1
# VITE_WS_URL=wss://api.vortex.timant32.ru/ws
# VITE_AGENT_CORE_URL=wss://api.vortex.timant32.ru
