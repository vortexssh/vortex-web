# Agent binaries

Served at `https://<vortex-web>/agent/vortex-agent-linux-amd64` (and arm64).

- **Production Docker:** compiled into the image automatically from `VortexAgent/`.
- **Local Vite:** `cd ../VortexAgent && make publish-web`
